---
slug: "entete-univers"
title: "En-tête d'univers : les trois gestes (#71, #72, #73)"
created: 2026-08-25
status: "In Progress"
---

# Journal de développement

## Progression

| Tâche | Statut | Date |
|-------|--------|------|
| 1. `packages/shared` : `summarizeUniverse` + tests dans `layout.test.ts` | En attente | |
| 2. `apps/web/src/dnd/gap.ts` : `dropTarget` + tests dans `gap.test.ts` | En attente | |
| 3. #71 — `onGlobal` prend une `Scope` (`App.tsx`, `Matrix.tsx`) ; le nom d'univers devient un bouton (+ CSS) | En attente | |
| 4. #72 — chevron `aria-expanded`, repli, persistance `localStorage`, résumé de l'en-tête replié (+ CSS) | En attente | |
| 5. #72 — dossier à ressort : dépliage au survol prolongé pendant un glisser de matrice | En attente | |
| 6. #73 — `drag` en union discriminée + gardes `kind` sur toutes les zones de dépôt existantes | En attente | |
| 7. #73 — poignée `.uni-head__grip`, interstices d'univers, réordonnancement au dépôt (+ CSS) | En attente | |
| 8. Trou tactile — `sheet` en union discriminée, appui long sur l'en-tête d'univers (+ CSS) | En attente | |
| 9. Trou tactile — `.uni-head__actions` : correctif `(hover: none)` sur le motif de #38 | En attente | |
| 10. Portes qualité : `typecheck` + `test` + `build` sur les 3 workspaces | En attente | |

## Journal

<!-- Les entrées seront ajoutées ici au fur et à mesure du développement -->
