node_modules/
dist/
build/
*.local
.env
.env.*
!.env.example
.DS_Store
.vite/
apps/extension/dist/
apps/web/dist/
# Paquet du Chrome Web Store : artefact de build, reproductible via `npm run build:ext`.
apps/extension/*.zip
coverage/
*.log

# Supabase (artefacts locaux du CLI)
apps/supabase/.branches/
apps/supabase/.temp/
apps/supabase/.env
# Le CLI écrit aussi à la RACINE selon d'où il est lancé (`supabase/` sans
# `--workdir apps`). `cli-latest` y était suivi et salissait chaque git status.
supabase/.branches/
supabase/.temp/

# Outillage local de Claude Code (worktrees, état de session). Jamais versionné :
# un worktree est une vue sur le dépôt, pas un contenu du dépôt.
.claude/
